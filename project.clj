(defproject pipe.dream "0.0.1"
  :description "Elixir-like pipe `|>` in Clojure(Script) that supports thread-first, thread-last, and as->."
  :dependencies [[org.clojure/clojure "1.10.1"]]
  :test-paths ["test"])
